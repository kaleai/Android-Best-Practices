<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kale.toolbox" >

    <application
        android:allowBackup="true"
        android:name=".base.BaseApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".debug.log.LoggerActivity"
            android:label="Log"/>

        <activity android:name=".ui.view.EasyToastActivity"/>
        
        <activity
            android:name=".ui.util.EditTextActivity"
            android:label="@string/title_activity_edit_util_test2" >
        </activity>

        
        
    </application>

</manifest>
